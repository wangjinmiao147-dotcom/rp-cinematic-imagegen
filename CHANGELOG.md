# v2.9.22 — Android 真机定位与参考图错误处理

- 根据小米浏览器真机调试修复零高度、带 transform/perspective 的根元素导致悬浮球出屏；处理 visualViewport 变化。
- 悬浮球初始化异常保留堆栈；参考图读取、下载、edits HTTP/网络错误不再静默降级文生图。
- 区分 edits 404 路由缺失、405/501 不支持、网络原因待诊断、临时成图下载失败。
- 修复参考图库读写失败/中止回调，增加完整图片参考图包导出与导入。
- 新增行为测试、IndexedDB 故障测试及可重复的 Android CDP 真机测试。详见 docs/android-debugging.md。

# 更新日志

本项目遵循 [Semantic Versioning](https://semver.org/lang/zh-CN/)。

## [2.9.21] - 2026-09-05

- 修复 PC 与手机因新旧扩展配置键并存而使用不同图片模型、出现明显画质差异的问题。
- 正式配置尚未完成时自动迁移 `rp-cinematic-v2` 旧配置；正式配置已经可用时绝不被旧模型、旧地址或旧密钥覆盖。
- 保留 v2.9.20 的结构化焦点分析、兼容中转响应格式及一次 JSON 校正重试。

## [2.9.20] - 2026-09-05

- 酒馆助手 JSON 改为默认启用：手机导入后立即检查、安装或更新完整版，不再只显示一个没有实际监听器的按钮。
- 已安装正式版时保留“安装 / 更新”按钮供手动检查更新，避免每次载入页面触发自动刷新循环。
- 焦点镜头 LLM 返回普通文本、非标准 JSON 或缺少 `final_prompt` 时，自动解析原文或使用当前剧情兜底，不再直接终止生图。
- 安装按钮增加版本号，便于手机端一眼确认当前导入的是不是最新安装器。

## [2.9.19] - 2026-09-05

- 修复酒馆助手 JSON 在检测到旧目录 `rp-cinematic-v2` 时只提示、不安装也不更新的问题。
- 一键安装器现在先从 GitHub 安装完整正式版，确认成功后再移除旧目录并刷新，避免手机端长期加载旧脚本。
- 已用 390×844 手机视口完成端到端验证：展开悬浮面板、立即出图、五阶段状态、提示词确认、参考图失败降级和最终图片挂载均可用。

## [2.9.18] - 2026-09-05

- 修复手机端打开最终提示词预览时自动聚焦文本框、立即弹出软键盘的问题。
- 移动端提示词预览改用动态视口高度、独立平滑滚动和安全区适配，避免浏览器地址栏与输入法挤压内容。
- “确认并生成”改为手机端底部固定主操作，恢复原稿与取消按钮紧随其后，无需滚到长提示词最底端。
- 预览开启期间锁定背景滚动，关闭、取消、确认或任务中止时自动恢复。

## [2.9.17] - 2026-09-05

- 修复最终总结 LLM 的安全策略/敏感词拦截文案被误当作有效生图提示词的问题。
- 检测到策略拦截、拒绝或空结果时，自动降级合并导演稿与上帝视角稿，保留正确的场景、人物性别、动作和构图。
- 新增中英文策略拦截识别回归测试，避免只剩人数约束和女性参考图时把男性误生成成第二名女性。

## [2.9.16] - 2026-09-05

- 修复演员表标准化逻辑会无条件加入“用户 + 当前角色”、导致单人场景被锁成双人构图的问题。
- 上帝视角改用“身体在场证据”判定：群成员、角色卡存在、最近发言、仅被提及或说话人身份均不再自动入镜；证据不足默认排除。
- 新增明确不入镜名单，最终总结与图片提示词会彻底排除离场或镜头外人物。
- 最终提示词预览新增“明确不入镜”信息，生成前可直接核对群像人数。

## [2.9.15] - 2026-09-05

- 手机端改为直接复用桌面端可拖动 🎬 悬浮球和完整工作台，不再使用位于文档流中的独立快捷操作条。
- 悬浮球固定在当前手机视口右下方；打开后，主动出图、镜头、视角、画幅、画风、连续性、自动出图、队列、图库和设置均与电脑端一致。
- 手机端面板增加视口高度、安全区、滚动和触控尺寸适配。

## [2.9.14] - 2026-09-05

- 新增手机端常驻快捷操作条，“🎬 出图”可直接为当前会话最新 AI 消息主动生成配图，“⚙ 面板”可打开完整控制面板。
- 手机端控制面板适配安全区、可视高度和纵向滚动，避免按钮被底部输入栏遮挡。
- 强化手机端消息内 🎬 与图库按钮的触控尺寸及可见性。

## [2.9.13] - 2026-09-05

- 酒馆助手安装器检测到扩展已安装时改为静默，不再在每次酒馆启动时重复弹出提示；首次安装、手动更新和错误仍会明确提示。

## [2.9.12] - 2026-09-05

- 新增可直接导入酒馆助手的 JSON 安装器；导入为全局脚本并启用后，会自动从官方 GitHub 仓库安装扩展并刷新页面。
- 安装器提供“安装 / 更新 RP 电影配图”按钮，已安装用户可直接检查更新。
- 发布构建现在会同时生成原生扩展 ZIP、酒馆助手 JSON 以及各自的 SHA-256 校验文件，并自动清理旧版构建产物。
- 酒馆助手 JSON 不包含 API Key、用户设置、聊天记录、角色卡或生成图片。

## [2.9.11] - 2026-09-05

- 最终提示词预览窗口点击外部遮罩时不再意外关闭；仅明确点击取消、关闭按钮或按 `Esc` 才会取消任务。
- 点击窗口外部时增加轻微边框与位移动画，提示用户需要在窗口内完成操作。

## [2.9.10] - 2026-09-05

- 修正 GitHub Windows / Node 20 环境下测试文件路径的兼容性，确保自动构建与 Release 正常执行。

## [2.9.9] - 2026-09-05

### 新增与改进

- 手动生成在调用图片后端前展示最终正向与负向提示词，支持直接修改、恢复 AI 原稿或取消。
- 新增单任务串行队列，连续触发时不再丢任务，并显示当前任务、排队列表与数量。
- 新增“取消当前”和“清空排队”操作；取消信号会传递到 LLM、OpenAI、Gemini、SD、ComfyUI 与酒馆内置 SD 调用。
- 自动出图保持无人值守运行，不弹出提示词确认窗口。

## [2.9.8] - 2026-09-05

### 新增与改进

- 重构现代深色电影工作台、设置卡片、悬浮面板和图库灯箱。
- 强化上帝视角的人物调度、多人统一构图和演员表约束。
- 优化角色原图身份锚定、上一镜头连续性与场景切换规则。
- 增强 OpenAI 兼容图片编辑接口的参考图处理、MIME 类型和失败重试。
- 统一稳定入口为 `index.js`，补齐开源发布、自动校验与 GitHub Release 流程。

## [2.9.6] - 2026-09-05

- 优先读取完整角色原图，避免低分辨率缩略图影响锁脸。
- 图片编辑上传按真实 MIME 类型设置文件名。
- 参考图失败时自动更换参考图重试，最终才降级为文字生图。

## [2.9.5] - 2026-09-05

- 角色卡原图固定为第一优先级身份锚点。
- 上一张剧情图仅继承未改变的造型和道具状态。
- 强化换装、换场景、远景与全身构图下的面部一致性。

## [2.9.0] - 2026-09-04

- 引入焦点镜头、上帝视角核对和最终提示词总结三段式流程。
- 支持独立配置最终总结 LLM。
