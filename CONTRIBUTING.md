# 贡献指南

感谢你帮助改进 RP 电影配图。

## 开发流程

1. Fork 仓库并从 `main` 创建功能分支。
2. 修改后运行 `npm run check`。
3. 在 SillyTavern 最新 release 中手动验证设置面板、消息按钮、悬浮工作台和至少一个图片后端。
4. 提交 Pull Request，说明变更目的、验证环境和界面变更截图。

## 基本要求

- 不得提交 API Key、Cookie、访问令牌、用户设置、聊天记录、角色卡或生成图片。
- 不得提交备份目录、历史版本入口、`dist/` 或本机绝对路径。
- 用户可见行为发生变化时，请同步更新 README 和 CHANGELOG。
- `manifest.json`、`package.json` 与界面显示的版本号应保持一致。
- 对网络请求的修改必须说明目标地址、发送的数据和密钥处理方式。

## Issue

Bug 报告请包含 SillyTavern 版本、浏览器、复现步骤和已脱敏的控制台错误。不要公开粘贴任何密钥或完整个人数据。
